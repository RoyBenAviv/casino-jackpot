# Optional — the server runs with sensible defaults when no .env exists.
# Copy this file to `.env` to override. Read only by `npm start`
# (node --env-file-if-exists); dev uses the defaults in src/config/env.ts.
PORT=3000
CLIENT_ORIGIN=http://localhost:5173
